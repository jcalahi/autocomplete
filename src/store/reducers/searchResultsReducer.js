const defaultState = {
  searchResults: []
};

const searchResultReducer = (state = defaultState, action) => {
  switch(action.type) {
    default:
      return state;
  }
}

export default searchResultReducer;
